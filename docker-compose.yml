services:
  app:
    build:
      args:
        user: 1002
        uid: 1002
      context: ./
      dockerfile: Dockerfile
    image: sce
    container_name: sce
    restart: always
    working_dir: /var/www/
    volumes:
      - /srv/webhost/studentcouncilelection/www/:/var/www
    networks:
      - link0

networks:
  link0:
    external: true
